[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && . /opt/venv/bin/activate && python manage.py migrate && gunicorn invest_backend.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/health/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
