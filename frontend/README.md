# Invest App - Frontend

Scaffold minimal React + TypeScript frontend for the investment application.

Getting started (Windows / PowerShell):

```powershell
npm install
npm run dev
```

Environment variables:
- `VITE_API_BASE_URL` — base url of the Laravel API (e.g. `http://localhost:8000/api`).

The project includes a basic Auth context, axios API wrapper and simple Login/Register/Dashboard pages to bootstrap development.
