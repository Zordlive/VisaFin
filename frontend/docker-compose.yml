version: '3.8'
services:
  frontend:
    image: node:20
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - '5173:5173'
    environment:
      - VITE_API_BASE_URL=http://host.docker.internal:8000/api
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
